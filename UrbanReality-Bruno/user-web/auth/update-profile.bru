meta {
  name: update-profile
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/web/user/auth/update-profile
  body: json
  auth: inherit
}

body:json {
  {
    "firstName": "Abh",
    "lastName": "Kumar",
    "logo": "<base64encodedfile>"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
